This module depends on `liza_base` and `account`. Liza credentials
must be configured in `liza_base` before this module can send data.

The AutoPayex API URL is set automatically on installation
(`liza.payex.url` system parameter). It can be adjusted in
Settings > Technical > System Parameters if needed.
